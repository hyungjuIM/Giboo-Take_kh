<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">

    <!-- 결과 매핑 -->
    <resultMap type="member" id="member_rm">
        <id property="memberNo" column="MEMBER_NO"/>
        <result property="memberId" column="MEMBER_ID"/>
        <result property="memberPw" column="MEMBER_PW"/>
        <result property="memberEmail" column="MEMBER_EMAIL"/>
        <result property="memberName" column="MEMBER_NAME"/>
        <result property="memberNick" column="MEMBER_NICK"/>
        <result property="memberTel" column="MEMBER_TEL"/>
        <result property="memberAddr" column="MEMBER_ADDR"/>
        <result property="enrollDt" column="ENROLL_DT"/>
        <result property="secessionFlag" column="SECESSION_FL"/>
        <result property="profileImg" column="PROFILE_IMG"/>
        <result property="memberType" column="MEMBER_TYPE"/>
        <result property="rateNo" column="RATE_NO"/>
        <result property="pointNo" column="POINT_NO"/>
    </resultMap>
    <resultMap type="manager" id="manager_rm">
    	<id property="mgrNo" column="MGR_NO"/>
    	<result property="mgrId" column="MGR_ID"/>
    	<result property="mgrPw" column="MGR_PW"/>
    	<result property="mgrEmail" column="MGR_EMAIL"/>
    	<result property="mgrName" column="MGR_NAME"/>
    	<result property="mgrNickname" column="MGR_NICKNAME"/>
    	<result property="mgrTel" column="MGR_TEL"/>
    	<result property="mgrPermission" column="MGR_PERMISSION"/>
    	<result property="mgrProfileImg" column="MGR_PROFILE_IMG"/>
    	<result property="mgrSignUpStatus" column="MGR_SIGNUP_STATUS"/>
    	<result property="mgrSignUpReason" column="MGR_SIGNUP_REASON"/>
    </resultMap>
    
    <!--  이메일 인증 -->
	<resultMap type="emailCertification" id="emailcertification_rm">
		<id property="email" column="EMAIL"/>
		<result property="certificationNo" column="CERTIFICATION_NO" />
		<result property="issueDate" column="ISSUE_DT"/>
	</resultMap>

    <!-- 로그인 쿼리 -->
    <select id="login" parameterType="member" resultMap="member_rm">
        SELECT MEMBER_NO, MEMBER_ID, MEMBER_PW, MEMBER_EMAIL, MEMBER_NAME, MEMBER_NICK,
        MEMBER_TEL, MEMBER_ADDR,
        TO_CHAR(ENROLL_DT,'YYYY-MM-DD HH24:MI:SS') AS ENROLL_DT,
        PROFILE_IMG,MEMBER_TYPE,RATE_NO,POINT_NO
        FROM MEMBER
        WHERE MEMBER_ID = #{memberId}
        AND SECESSION_FL = 'N'
    </select>
	
	<!-- 매니저 로그인 -->
	<select id="managerLogin" parameterType="manager" resultMap="manager_rm">
		SELECT MGR_NO, MGR_ID, MGR_PW, MGR_EMAIL,
		MGR_NAME, MGR_NICKNAME, MGR_TEL,MGR_PERMISSION,
		MGR_PROFILE_IMG,MGR_SIGNUP_REASON
		FROM MANAGER
		WHERE MGR_ID = #{memberId}
		AND MGR_SIGNUP_STATUS = 'N'
	</select>
	
	<!-- 이메일 인증 -->
	<update id="updateCertification">
		UPDATE EMAILCERTIFICATION SET
		CERTIFICATION_NO = #{certificationNo},
		ISSUE_DT = SYSDATE
		WHERE EMAIL = #{email}
		
	</update>
	<insert id="insertCertification">
		INSERT INTO EMAILCERTIFICATION(CERTIFICATION_NO, ISSUE_DT) VALUES({#memberEamil},#{certificationNo), SYSDATE)
	</insert>
	
	
</mapper>
 