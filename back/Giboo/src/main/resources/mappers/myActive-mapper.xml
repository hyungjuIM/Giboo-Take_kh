<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="myactiveMapper">

	<!-- 나의활동 목록 조회 -->
	<resultMap type="myactive" id="myactive_rm">
		<id property="myactiveNo" column="MYACTIVE_NO"/>
		<result property="volunteerCompleteStatus" column="VOLUNTEER_COMPLETE_STATUS"/>
		<result property="donationNo" column="DONATION_NO"/>
		<result property="volunteerNo" column="VOLUNTEER_NO"/>
		<result property="memberNo" column="MEMBER_NO"/>
		<result property="eventNo" column="EVENT_NO"/>
	</resultMap>
	
	<!-- 기부 목록 조회 -->

	
	<!-- 기부 목록 조회 -->
	<select id="selectMyactiveDonationList" >
		SELECT DONATION_NO, DONATION_TITLE
		FROM DONATION
		JOIN MEMBER USING(MEMBER_NO)
	</select>
</mapper>
